hifiberry-vu-meter (0.1.2) stable; urgency=medium

  * Ship the auth-gateway classification manifest (etc/hifiberry/auth.d/vu-meter.json).
    The VU meter is a read-only audio-level stream, so the whole service is
    "ok" (never prompts for a password). Previously the manifest existed only
    as an on-device file shipped by no package.
  * Add curl + ca-certificates to Build-Depends. debian/rules rustup-installs
    the Rust toolchain via curl, which is not in the current build chroot; the
    build failed with "curl: not found" without this.

 -- HiFiBerry <support@hifiberry.com>  Fri, 17 Jul 2026 14:00:00 +0000

hifiberry-vu-meter (0.1.1) stable; urgency=medium

  * Install nginx include into /etc/nginx/hifiberry-api.d/
  * Remove legacy include from both old and new locations on package removal

 -- HiFiBerry <support@hifiberry.com>  Fri, 27 Feb 2026 23:10:00 +0000

hifiberry-vu-meter (0.1.0) stable; urgency=medium

  * Initial release
  * PipeWire audio level capture with binary WebSocket streaming
  * Supports stereo L/R RMS, peak, and clipping detection
  * Lightweight 6-byte binary protocol at 10Hz update rate
  * REST version endpoint at /api/v1/version
  * Nginx reverse proxy configuration for WebSocket support

 -- HiFiBerry <support@hifiberry.com>  Thu, 27 Feb 2026 20:00:00 +0000
